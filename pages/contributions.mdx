import PageLayout from '@/layouts/PageLayout';

Recognising People who have given Feature Suggestions to improve this Application.

-   **Rohini**
    -   moving `upload config` in Edit Tab
    -   route to Country Map on clicking the Landing Map
-   **Ashris** : adding and placing `labels` on Maps
-   **Franco** : export maps to `SVG | PDF`
-   **Liam** : french map based on `regions` instead of `departments`

export default ({ children }) => (
    <PageLayout title="Contributions" showNav>
        {children}
    </PageLayout>
);
